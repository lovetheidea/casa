{%- assign headerSnippetPath = "header-index" -%}{%- assign wfPage = "636e30d70d7d566b406c3db7" -%}{%- assign bodyClasses = "" -%}
  {%- render 'global-styles' -%}
  {%- render 'header' -%}
  {%- section 'navigation' -%}
  {%- render 'mobile-nav' -%}
  <div class="page_wrapper margin_40">
    <div class="page_padding">
      <div class="services_wrapper">
        <div class="services_container wide">
          <div class="content_vertical short">
            <h1 class="heading_m">{{ 'tn585ec447' | t }}</h1>
            <div class="content_horizontal">
              <a href="{{ routes.account_url }}" aria-current="page" class="button_line_blue w-button w--current">{{ 'tn72bbb0fb' | t }}</a>
              <a href="{{ routes.account_addresses_url }}" class="button_line_blue w-button">{{ 'tn546d647e' | t }}</a>
              <a href="/account/logout" class="button_line_blue w-button">{{ 't777ebc32' | t }}</a>
            </div>
            <p class="text_align-center">{{ 'tn518b5f63' | t }}<br>{{ 't745e123f' | t }}</p>
            <div class="services_content-wrapper">
              <h2 class="heading_r">{{ 'tn72bbb0fb' | t }}</h2>
              <div class="order-wrapper heading">
                <p id="w-node-_7bf7fcf4-65f6-1064-e870-a5243e8342c9-406c3db7" class="paragraph_filters">{{ 't48e972e' | t }}</p>
                <p id="w-node-c180a34f-064f-382b-9ac6-0e4a061e9115-406c3db7" class="paragraph_filters">{{ 't2063ce' | t }}</p>
                <p id="w-node-_42c20352-a6e2-4eb0-09e3-4744aa6cb172-406c3db7" class="paragraph_filters">{{ 't56af8b8c' | t }}</p>
                <p id="w-node-_66866eba-7de1-c982-1e75-2d86fcf170a9-406c3db7" class="paragraph_filters">{{ 'tn218063f8' | t }}</p>
                <p id="w-node-_9703645a-fba1-26d5-b5e9-b67349d440ea-406c3db7" class="paragraph_filters">{{ 't4d3eb24' | t }}</p>
              </div>
              {%- assign order = customer.last_order -%}{%- if order -%}<div class="order-wrapper">
                <a orde="url" href="{{ order.customer_url }}">{{ order.name }}</a>
                <div>{{ order.created_at | date: "%a, %b %d, %y" }}</div>
                <div id="w-node-_338fd124-40b0-5738-681a-9f755eac3370-406c3db7" class="content_horizontal">
                  <div>{{ order.financial_status_label }}</div>
                  {%- if order.cancelled -%}<div>{{ 'tn6c25b6cf' | t }}</div>{%- endif -%}
                </div>
                <div>{{ order.fulfillment_status_label }}</div>
                <div>{{ order.total_price | money }}</div>
              </div>{%- endif -%}
            </div>
          </div>
          <div class="divider full short"></div>
        </div>
      </div>
    </div>
    {%- section 'footer' -%}
  </div>
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=633ad4e42c7da0c6cfbbb8ac" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'webflow.js' | asset_url }}" type="text/javascript"></script>
  <!-- [if lte IE 9]><script src="https://cdnjs.cloudflare.com/ajax/libs/placeholders/3.0.2/placeholders.min.js"></script><![endif] -->
  <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@latest/bundled/lenis.js"></script>
  <script>
   const lenis = new Lenis({
  duration: 1.2,
  easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)), // https://www.desmos.com/calculator/brs54l4xou
  direction: 'vertical', // vertical, horizontal
  gestureDirection: 'vertical', // vertical, horizontal, both
  smooth: true,
  smoothTouch: false,
  touchMultiplier: 2,
  infinite: false,
})
//get scroll value
lenis.on('scroll', ({ scroll, limit, velocity, direction, progress }) => {
  console.log({ scroll, limit, velocity, direction, progress })
})
function raf(time) {
  lenis.raf(time)
  requestAnimationFrame(raf)
}
requestAnimationFrame(raf)
   </script>
  <!--  CUSTOM QUANTITY BUTTONS  -->
  <script>
const quantityGroupClass = "quan-group"
const quantityIncrementButtonClass = "quan-inc"
const quantityDecrementButtonClass = "quan-dec"
const quantityNumberFieldClass = "quan-num"
// Increment
$(document).on('click', `.${quantityIncrementButtonClass}`, function(){
  var $input = $(this).closest(`.${quantityGroupClass}`).find(`.${quantityNumberFieldClass}`);    
  var val = parseInt($input.val(), 10);
  $input.val(val + 1);
  $input[0].dispatchEvent(new Event('change'));
});
// Decrement
$(document).on('click', `.${quantityDecrementButtonClass}`, function(){
  var $input = $(this).closest(`.${quantityGroupClass}`).find(`.${quantityNumberFieldClass}`);    
  var val = parseInt($input.val(), 10);
  $input.val(Math.max(val - 1, 1));
  $input[0].dispatchEvent(new Event('change'));
});
</script>

{{ 'udesly-shopify.min.js' | asset_url | script_tag }}